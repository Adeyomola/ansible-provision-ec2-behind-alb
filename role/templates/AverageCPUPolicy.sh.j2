#!/usr/bin/bash
aws autoscaling put-scaling-policy --policy-name tt-cpu-scaling \
  --auto-scaling-group-name "{{ asg.auto_scaling_group_name }}" --policy-type TargetTrackingScaling \
  --target-tracking-configuration "file:///{{ role_path }}/files/ttconfig.json" --region "{{ region }}"
